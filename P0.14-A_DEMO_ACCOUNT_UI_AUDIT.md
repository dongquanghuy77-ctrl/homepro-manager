# P0.14-A DEMO ACCOUNT & UI VISIBILITY AUDIT

## 1. Mục Tiêu & Phương Pháp
- **Chỉ READ-ONLY**: Query vào bảng `users`, `roles`, `role_permissions` để lấy mappings thật sự.
- **Phân tích Luồng Auth**: Đối chiếu DB identity -> `auth/me` -> Visibility tại `Sidebar.tsx` -> Server Guard tại API `hr/payroll/route.ts`.

## 2. Kết quả quét tài khoản (Demo Accounts Audit)

| Username | Name | DB Role | Department | Active | Role Exists | Permissions (Payroll-related) | Expected UI | Actual UI (Sidebar) | API Scope (/api/hr/payroll) | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| **admin** | Quản trị viên | `ADMIN` | Quản lý | Yes | Yes | `payroll.view`, `payroll.read.all`, `payroll.create`, `payroll.publish`, etc. | Thấy Payroll | Thấy Payroll | ALL | 🟢 GREEN |
| **letramkt** | LÊ DƯƠNG NGỌC TRÂM | `ACCOUNTANT` | Kế toán | Yes | Yes | `payroll.view`, `payroll.read.all`, `payroll.create`, `payroll.publish` | Thấy Payroll | Thấy Payroll | ALL | 🟢 GREEN |
| **viewer** | Ban Giám Đốc (Xem) | `VIEWER` | Quản lý | Yes | Yes | `payroll.view`, `payroll.approve`, `payroll.export` | Thấy Payroll | Thấy Payroll | ALL | 🟢 GREEN |
| **hra** | HR | `HR` | (null) | Yes | Yes | `payroll.read.all`, `payroll.calculate` | **(No payroll)** | **Không thấy Payroll** | **ALL** | 🔴 RED (Lỗ hổng API) |
| **manager** | Huy - Quản lý xưởng | `MANAGER` | Quản lý | Yes | Yes | `payroll.read.department` | **(No payroll)** | **Không thấy Payroll** | **DEPARTMENT** | 🔴 RED (Lỗ hổng API) |
| **supervisor** | Nguyễn Văn Minh... | `MANAGER` | Xưởng gỗ | Yes | Yes | `payroll.read.department` | **(No payroll)** | **Không thấy Payroll** | **DEPARTMENT** | 🔴 RED (Lỗ hổng API) |
| **worker** | Trần Văn Thợ... | `WORKER` | Xưởng gỗ | Yes | Yes | `payroll.read.self` | Không thấy Payroll | Không thấy Payroll | SELF | 🟡 YELLOW (API thừa) |
| **phuc.tran** | TRẦN THANH PHÚC | `WORKER` | Xưởng gỗ | Yes | Yes | `payroll.read.self` | Không thấy Payroll | Không thấy Payroll | SELF | 🟡 YELLOW (API thừa) |
| **demo** | Tài khoản Demo | `STAFF` | (null) | Yes | Yes | `payroll.read.self` | Không thấy Payroll | Không thấy Payroll | SELF | 🟡 YELLOW (API thừa) |

## 3. Phân Tích Sự Bất Đồng Nhất (Mismatch Analysis)

### 🔴 RED - LỖ HỔNG BẢO MẬT (API Bypassing)
**HR và MANAGER có thể đọc trộm bảng lương qua API.**
- **Tại Frontend (UI)**: Ở P0 tôi đã update `Sidebar.tsx` yêu cầu có quyền `payroll.view` mới hiện menu Bảng lương. Role `HR` và `MANAGER` **không có** cờ này trong Database -> Menu bị ẩn. Giao diện trông có vẻ an toàn.
- **Tại Backend (API)**: Hàm `getPayrollReadScope` vẫn còn chứa fallback cũ:
  ```typescript
  if (await hasPermissionCode(session.role, 'payroll.read.all')) return 'ALL';
  if (await hasPermissionCode(session.role, 'payroll.read.department')) return 'DEPARTMENT';
  ```
  Và Database CŨ của Production vẫn đang map `HR` -> `payroll.read.all` và `MANAGER` -> `payroll.read.department`.
- **Hậu quả**: Bất kỳ User nào là HR (ví dụ: `hra`) hoặc MANAGER (ví dụ: `manager`, `supervisor`) nếu biết gọi trực tiếp API `GET /api/hr/payroll` (hoặc curl/postman) sẽ nhận được toàn bộ dữ liệu lương (ALL đối với HR, và toàn bộ phòng ban đối với Manager) mặc dù thiết kế đã cấm và UI đã ẩn.

### 🟡 YELLOW - LOGIC KHÔNG ĐỒNG NHẤT
**WORKER, STAFF, DESIGNER** có quyền `payroll.read.self`.
- Tại backend, quyền này trả về scope `SELF`. Do đó, nếu gọi API `/api/hr/payroll`, họ vẫn nhận được một array chứa đúng 1 phiếu lương của họ.
- Việc này tuy không làm rò rỉ dữ liệu người khác (nên không phải RED), nhưng nó dư thừa và không đúng quy chuẩn (xem phiếu lương cá nhân phải dùng endpoint riêng `/api/payroll/my` đã chặn IDOR, không dùng chung endpoint list của HR).

## 4. Kiểm tra riêng user `thanhphuc`
- `username`: `phuc.tran` (Tên đầy đủ: TRẦN THANH PHÚC).
- `role`: `WORKER`
- Không có lỗi UI (Menu ẩn hoàn toàn). Có lỗi 🟡 YELLOW (Gọi trực tiếp API list lương sẽ được phép xem của chính mình).

## 5. Kết luận Audit
- ❌ **UI_RBAC_MATCH**: NO (UI ẩn theo `payroll.view`, nhưng API mở theo `payroll.read.*`)
- ❌ **BACKEND_RBAC_MATCH**: NO (Backend vẫn chấp nhận các permissions cũ chưa bị xóa trong DB)
- ❌ **PAYROLL_SCOPE_MATCH**: NO (HR và Manager vẫn bypass được để xem lương qua API)
- ❌ **SECURITY_ISSUES**: YES (Nguy cơ lộ lọt dữ liệu lương qua API direct access đối với HR và Manager)
- 🟢 **PRODUCTION_MODIFIED**: NO (Hoàn toàn Read-Only)

*(Vui lòng xem thêm P0.14-A_DEMO_ACCOUNT_MATRIX.md để nắm chi tiết các quyền hạn chéo)*
