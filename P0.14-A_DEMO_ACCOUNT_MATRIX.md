# P0.14-A DEMO ACCOUNT MATRIX

Ma trận này phân tích chi tiết dữ liệu quyền từ DB, Identity sau đăng nhập, và khả năng hiển thị ở Frontend so với khả năng đọc ở Backend.

## A. DATA SCOPE MATRIX

| Role | Menu "Bảng Lương" (UI) | Gọi API `/hr/payroll` (List) | Xem Lương Cá Nhân (`/payroll/my`) | Calculate | Publish | Export | API Salary Access |
|---|---|---|---|---|---|---|---|
| **ADMIN / BOD** | Hiển thị | Toàn bộ (ALL) | Có | Có | Có (Nếu DB đã seed) | Có | **ALL** |
| **ACCOUNTANT** | Hiển thị | Toàn bộ (ALL) | Có | Có | Có (Nếu DB đã seed) | Có | **ALL** |
| **VIEWER** | Hiển thị | Toàn bộ (ALL) | Có | Không | Không | Có | **ALL** |
| **HR** | Ẩn (Block) | Toàn bộ (ALL) | Có | Có | Không | Không | 🔴 **ALL (API bypass)** |
| **MANAGER** | Ẩn (Block) | Department | Có | Không | Không | Không | 🔴 **DEPARTMENT (API bypass)** |
| **WORKER** | Ẩn (Block) | Self | Có | Không | Không | Không | 🟡 SELF (Nên block API này) |
| **SUPERVISOR** | Ẩn (Block) | Department | Có | Không | Không | Không | 🔴 **DEPARTMENT (API bypass)** |
| **STAFF** | Ẩn (Block) | Self | Có | Không | Không | Không | 🟡 SELF (Nên block API này) |
| **DESIGNER** | Ẩn (Block) | Self | Có | Không | Không | Không | 🟡 SELF (Nên block API này) |

## B. PERMISSIONS MAPPING TRONG DATABASE

Dưới đây là các quyền hiện đang được map cho từng Role trong DB (được lấy trực tiếp qua Query Read-Only):

**1. ADMIN**
`employee.read.all`, `employee.write.all`, `attendance.read.all`, `attendance.write.all`, `leave.read.all`, `leave.approve.all`, `payroll.read.all`, `payroll.calculate`, `payroll.approve`, `payroll.publish`, `expense.read.all`, `expense.write.all`, `payroll.view`, `payroll.create`, `payroll.edit`, `payroll.lock`, `payroll.export`.

**2. ACCOUNTANT**
`payroll.read.all`, `payroll.approve`, `expense.read.all`, `expense.write.all`, `payroll.view`, `payroll.create`, `payroll.edit`, `payroll.lock`, `payroll.export`.

**3. HR**
`employee.read.all`, `employee.write.all`, `attendance.read.all`, `attendance.write.all`, `leave.read.all`, `leave.approve.all`, `payroll.read.all`, `payroll.calculate`.
*(⚠️ Dư quyền payroll.read.all dẫn đến API Bypass)*

**4. MANAGER**
`employee.read.department`, `attendance.read.department`, `leave.read.department`, `leave.approve.department`, `payroll.read.department`.
*(⚠️ Dư quyền payroll.read.department dẫn đến API Bypass)*

**5. VIEWER**
`payroll.approve`, `payroll.view`, `payroll.export`.

**6. WORKER / DESIGNER / STAFF**
`employee.read.self`, `attendance.read.self`, `leave.read.self`, `payroll.read.self`.

## C. LOGIN / AUTHENTICATION STATE
Qua xác nhận, Identity được cấp phát qua JWT `session` chứa ID và Role khớp hoàn toàn với bảng `users` (dựa theo code lấy data từ `api/auth/me`). Khi user A login, UI và Session sẽ chính xác là của user A (lỗi hiển thị nhầm identity ở các build cũ đã được fix qua token validation). Không có dấu hiệu session bleeding ở hiện tại.
