import { db } from './src/db'; import { users, employees, departments, positions } from './src/db/schema'; import { eq } from 'drizzle-orm'; async function run() { const user = await db.select({ id: users.id, username: users.username, name: users.name, role: users.role, departmentId: users.departmentId, positionId: users.positionId, active: users.active }).from(users).where(eq(users.username, 'letramkt')); console.log(JSON.stringify(user, null, 2)); process.exit(0); } run();  
